---
sidebar_position: 6
title: खाता वार्मअप स्क्रिप्ट कॉन्फ़िगरेशन
description: खाता वार्मअप स्क्रिप्ट के लिए पूर्ण कॉन्फ़िगरेशन संदर्भ
---

यह पृष्ठ कार्य निर्माण के लिए `account_warmup` स्क्रिप्ट के कॉन्फ़िगरेशन पैरामीटर दस्तावेजित करता है।

## अवलोकन

`account_warmup` स्क्रिप्ट का उपयोग स्वाभाविक उपयोगकर्ता व्यवहार का अनुकरण करके TikTok या Instagram खातों को वार्मअप करने के लिए किया जाता है। यह वीडियो देखती है, और कॉन्फ़िगर की गई संभावनाओं के आधार पर यादृच्छिक रूप से लाइक, फ़ॉलो, संग्रह और टिप्पणी करती है। यह नए खातों को इंटरैक्शन हिस्ट्री बनाने और बॉट के रूप में पहचाने जाने से बचने में मदद करता है।

## स्क्रिप्ट कॉन्फ़िगरेशन (`script_config`)

`script_config` ऑब्जेक्ट में खाता वार्मअप स्क्रिप्ट के लिए पैरामीटर होते हैं। निम्नलिखित उपलब्ध पैरामीटर हैं:

### पैरामीटर

| पैरामीटर | प्रकार | आवश्यक | डिफ़ॉल्ट मान | विवरण |
|-----|------|------|-------|------|
| task_duration | number | नहीं | 600 | वार्मअप कार्य की कुल अवधि (सेकंड) |
| topic | string | नहीं | "" | खोज विषय कीवर्ड (प्रति पंक्ति एक, यादृच्छिक रूप से चयन) |
| min_duration | number | नहीं | 15 | न्यूनतम वीडियो देखने की अवधि (सेकंड) |
| max_duration | number | नहीं | 30 | अधिकतम वीडियो देखने की अवधि (सेकंड) |
| like_probable | number | नहीं | 0 | वीडियो लाइक करने की संभावना (0-100) |
| floow_probable | number | नहीं | 0 | वीडियो निर्माता को फ़ॉलो करने की संभावना (0-100) |
| collect_probable | number | नहीं | 0 | वीडियो संग्रह/बुकमार्क करने की संभावना (0-100) |
| comment_probable | number | नहीं | 0 | वीडियो पर टिप्पणी करने की संभावना (0-100) |
| comment | string | नहीं | "" | टिप्पणी टेम्पलेट (प्रति पंक्ति एक, यादृच्छिक रूप से चयन) |
| insert_emoji | boolean | नहीं | false | टिप्पणियों में यादृच्छिक इमोजी सम्मिलित करें या नहीं |
| comment_order | string | नहीं | "random" | टिप्पणी चयन क्रम: `random` (यादृच्छिक) या `sequential` (क्रमिक) |
| generate_by_chatgpt | boolean | नहीं | false | ChatGPT का उपयोग करके टिप्पणियां उत्पन्न करें या नहीं |
| chatgpt_settings | object | नहीं | {} | ChatGPT कॉन्फ़िगरेशन सेटिंग्स (नीचे देखें) |

### ChatGPT सेटिंग्स संरचना

जब `generate_by_chatgpt` को `true` पर सेट किया जाता है, तो आप ChatGPT टिप्पणी निर्माण को कॉन्फ़िगर करने के लिए `chatgpt_settings` ऑब्जेक्ट का उपयोग कर सकते हैं:

| पैरामीटर | प्रकार | आवश्यक | विवरण |
|-----|------|------|------|
| api_key | string | हां | आपकी OpenAI API कुंजी |
| model | string | नहीं | उपयोग करने के लिए मॉडल (डिफ़ॉल्ट: "gpt-3.5-turbo")। विकल्प: "gpt-3.5-turbo", "gpt-4", "gpt-4-turbo" |
| prompt | string | नहीं | टिप्पणियां उत्पन्न करने के लिए कस्टम प्रॉम्प्ट। डिफ़ॉल्ट रूप से मित्रवत, प्रासंगिक टिप्पणियां उत्पन्न करता है |
| max_tokens | number | नहीं | प्रतिक्रिया के लिए अधिकतम टोकन संख्या (डिफ़ॉल्ट: 100) |
| temperature | number | नहीं | रचनात्मकता स्तर 0-2 (डिफ़ॉल्ट: 0.7)। उच्च मान = अधिक रचनात्मक |
| base_url | string | नहीं | कस्टम API एंडपॉइंट URL (Azure OpenAI या संगत API के लिए) |

`chatgpt_settings` ऑब्जेक्ट उदाहरण:

```json
{
  "api_key": "sk-your-openai-api-key",
  "model": "gpt-3.5-turbo",
  "prompt": "इस वीडियो के लिए एक छोटी मित्रवत हिंदी टिप्पणी उत्पन्न करें",
  "max_tokens": 50,
  "temperature": 0.8,
  "base_url": "https://api.openai.com/v1"
}
```

:::tip सुझाव
नए खातों के लिए, कम इंटरैक्शन संभावनाओं (5-15%) से शुरू करने और फिर समय के साथ धीरे-धीरे बढ़ाने की सिफारिश की जाती है। यह स्वाभाविक उपयोगकर्ता व्यवहार का अनुकरण करता है।
:::

## उदाहरण

### बुनियादी खाता वार्मअप

केवल वीडियो देखने वाला सरल वार्मअप:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 600,
      "min_duration": 10,
      "max_duration": 30
    }
  }'
```

### विषय खोज के साथ वार्मअप

विशिष्ट विषयों की खोज के माध्यम से खाता वार्मअप करें:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 900,
      "topic": "मजेदार बिल्लियां\nकुत्ते के वीडियो\nपालतू संग्रह",
      "min_duration": 15,
      "max_duration": 45
    }
  }'
```

### इंटरैक्शन के साथ वार्मअप

पूर्ण वार्मअप, लाइक, फ़ॉलो और टिप्पणियां शामिल:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 1200,
      "topic": "भोजन\nरेसिपी\nखाना पकाना",
      "min_duration": 20,
      "max_duration": 60,
      "like_probable": 30,
      "floow_probable": 10,
      "collect_probable": 5,
      "comment_probable": 15,
      "comment": "बहुत बढ़िया! 🔥\nइस सामग्री को पसंद किया!\nबहुत अच्छा! 👏\nवाह, बहुत शानदार!",
      "insert_emoji": true,
      "comment_order": "random"
    }
  }'
```

### ChatGPT टिप्पणियों के साथ वार्मअप

ChatGPT का उपयोग करके बुद्धिमान टिप्पणियां उत्पन्न करें:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 1800,
      "topic": "तकनीकी समीक्षा\nडिजिटल उत्पाद",
      "min_duration": 30,
      "max_duration": 90,
      "like_probable": 25,
      "comment_probable": 20,
      "generate_by_chatgpt": true,
      "chatgpt_settings": {
        "api_key": "your-api-key",
        "model": "gpt-3.5-turbo",
        "prompt": "इस वीडियो के लिए एक छोटी मित्रवत टिप्पणी उत्पन्न करें"
      }
    }
  }'
```

### कई डिवाइस पर बैच वार्मअप

कई डिवाइस पर एक साथ वार्मअप चलाएं:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_1", "device_2", "device_3"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 600,
      "min_duration": 15,
      "max_duration": 30,
      "like_probable": 20
    },
    "enable_multi_account": true
  }'
```

### निर्धारित वार्मअप कार्य

विशिष्ट समय पर वार्मअप चलाने के लिए शेड्यूल करें:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 1200,
      "topic": "संगीत\nनृत्य\nट्रेंडिंग",
      "min_duration": 20,
      "max_duration": 40,
      "like_probable": 15,
      "floow_probable": 5
    },
    "start_time": "09:00"
  }'
```

### उपयोगकर्ता नाम सूची द्वारा वार्मअप

विशिष्ट खातों के लिए वार्मअप कार्य बनाएं:

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "usernames": ["@my_account1", "@my_account2"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 600,
      "min_duration": 15,
      "max_duration": 30,
      "like_probable": 20,
      "floow_probable": 5
    }
  }'
```

## प्रतिक्रिया

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "task_ids": [301, 302, 303],
    "created_count": 3
  }
}
```

## सर्वोत्तम प्रथाएं

1. **कम संभावनाओं से शुरू करें**: नए खातों के लिए, कम इंटरैक्शन संभावनाओं (5-15%) का उपयोग करें, फिर कुछ दिनों/सप्ताहों में धीरे-धीरे बढ़ाएं।

2. **प्रासंगिक विषयों का उपयोग करें**: अपने खाते की स्थिति से संबंधित विषयों का चयन करें ताकि प्रासंगिक इंटरैक्शन हिस्ट्री बना सकें।

3. **देखने की अवधि को बदलें**: स्वाभाविक देखने के पैटर्न का अनुकरण करने के लिए min_duration और max_duration के बीच सीमा सेट करें।

4. **मध्यम कार्य अवधि**: निरंतर लंबे सत्रों के बजाय प्रति दिन 2-3 बार, प्रत्येक 10-30 मिनट के वार्मअप सत्र चलाएं।

5. **विविध टिप्पणियों का उपयोग करें**: स्पैम डिटेक्शन को ट्रिगर कर सकने वाले दोहराए गए पैटर्न से बचने के लिए कई टिप्पणी टेम्पलेट प्रदान करें।

6. **समझदारी से शेड्यूल करें**: लक्ष्य दर्शकों के समय क्षेत्र में सक्रिय समय के दौरान वार्मअप कार्य चलाने के लिए `start_time` का उपयोग करें।

## और देखें

- [कार्य प्रबंधन API](./task-management.md) - कार्यों को बनाएं, सूचीबद्ध करें और प्रबंधित करें
- [Post स्क्रिप्ट कॉन्फ़िगरेशन](./post-script.md) - Post स्क्रिप्ट पैरामीटर कॉन्फ़िगर करें
- [फ़ॉलो स्क्रिप्ट कॉन्फ़िगरेशन](./follow-script.md) - फ़ॉलो स्क्रिप्ट पैरामीटर कॉन्फ़िगर करें
- [अनफ़ॉलो स्क्रिप्ट कॉन्फ़िगरेशन](./unfollow-script.md) - अनफ़ॉलो स्क्रिप्ट पैरामीटर कॉन्फ़िगर करें
