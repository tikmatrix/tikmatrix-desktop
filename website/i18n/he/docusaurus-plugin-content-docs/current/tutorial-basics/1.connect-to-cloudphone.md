---
sidebar_position: 2
---

# חיבור לטלפון ענן

מדריך זה יעזור לך להתחבר לטלפון ענן GeeLark באמצעות TikMatrix.

:::warning הערה לגבי רשת
אנא וודא שרוחב הפס של הרשת בין המחשב שלך לחדר השרתים של טלפון הענן הוא מספיק ויציב. לקבלת תוצאות מיטביות, מומלץ למקם את המחשב וחדר השרתים של טלפון הענן באותה מדינה או אזור כדי להפחית השהיה ואובדן מנות, ובכך להבטיח הרצה יציבה ואמינה של משימות אוטומטיות.
:::

ניתן לרכוש טלפון ענן מ-[GeeLark](https://www.geelark.cn?invite_code=XHY6a8).

## הפעלת ADB

הפעל את ADB בפרופיל טלפון הענן הרצוי בחלק הפרופילים.

![הפעלת ADB](../img/enable-adb.webp)

ניתן גם להפעיל את ADB עבור מספר פרופילים בו-זמנית.
![הפעלת ADB עבור מספר פרופילים](../img/enable-adb-multiple.webp)

## הפעלת הפרופיל

לאחר פתיחת הפרופיל, לחץ על סמל 🔗 כדי לקבל את כתובת ה-IP, הפורט וקוד החיבור של טלפון הענן.
![קבלת כתובת IP](../img/get-ip-address.webp)

## שיטה 1: חיבור מהיר באמצעות רשימת טלפוני ענן (מומלץ)

TikMatrix תומך כעת בשמירת מידע חיבור לטלפון ענן להתחברות מהירה בלחיצה אחת.

### שמירת מידע טלפון ענן

1. פתח את TikMatrix ולחץ על הכפתור `סריקת התקני TCP`.
2. עבור ללשונית `רשימת טלפוני ענן`.
3. לחץ על כפתור `הוסף`.
4. מלא את מידע טלפון הענן:
   - **שם**: תן שם מזהה לטלפון הענן (לדוגמה: "GeeLark טלפון 1")
   - **כתובת IP**: כתובת ה-IP שהתקבלה מ-GeeLark (לדוגמה: `1.2.3.4`)
   - **פורט**: מספר הפורט (ברירת מחדל: `5555`)
   - **קוד חיבור**: קוד החיבור שהתקבל מ-GeeLark (לדוגמה: `f850ef`)
5. לחץ על `שמור`.

### התחברות בלחיצה אחת

לאחר השמירה, ניתן להתחבר בשתי דרכים:

**חיבור בודד:**

- לחץ על כפתור 🔌 ליד כל טלפון ענן ברשימת טלפוני הענן.

**חיבור מרובה:**

- השתמש בתיבות הסימון כדי לבחור מספר טלפוני ענן.
- לחץ על הכפתור `התחבר (X)` בחלק העליון.

המערכת תשלים אוטומטית את הפעולות הבאות:

1. חיבור דרך ADB
2. ביצוע פקודת אימות glogin
3. הצגת תוצאות החיבור

לאחר חיבור מוצלח, רענן את רשימת המכשירים כדי לראות את טלפון הענן שלך.

## שיטה 2: חיבור ידני דרך מסוף ADB

אם אתה מעדיף חיבור ידני או צריך לפתור בעיות:

### הרצת פקודות ADB

1. ב-TikMatrix לחץ על `הגדרות` > `פתיחת מסוף ADB`.
2. הפעל את פקודת החיבור המתאימה למערכת ההפעלה שלך, ובדוק שהטלפון שלך מופיע ברשימה עם הסטטוס `device`.
3. הפעל את פקודת האימות כדי לאמת את החיבור.

### Windows

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### Mac/Linux

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> לדוגמה, אם כתובת ה-IP שלך היא `1.2.3.4`, הפורט הוא `5555`, וקוד החיבור הוא `f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## עצות

- שמור את כל טלפוני הענן ברשימה לגישה מהירה.
- סטטוס החיבור יוצג ליד כל טלפון ענן.
- ניתן לערוך או למחוק טלפוני ענן שמורים בכל עת.
- מידע החיבור מאוחסן במחשב המקומי שלך.
