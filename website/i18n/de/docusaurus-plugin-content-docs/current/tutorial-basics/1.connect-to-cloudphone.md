---
sidebar_position: 2
---

# Verbindung zu Cloud-Telefonen

Diese Anleitung hilft Ihnen, GeeLark Cloud-Telefone mit TikMatrix zu verbinden.

:::warning Netzwerkempfehlungen
Bitte stellen Sie sicher, dass Ihr Computer und das CloudPhone-Rechenzentrum über ausreichende und stabile Netzwerkbandbreite verfügen. Für beste Zuverlässigkeit der Automatisierungsaufgaben wird empfohlen, dass sich Ihr Computer und das Cloud-Telefon-Rechenzentrum im selben Land oder derselben Region befinden, um Latenz und Paketverlust zu reduzieren.
:::

Sie können Cloud-Telefone bei [GeeLark](https://www.geelark.cn?invite_code=XHY6a8) erwerben.

## ADB aktivieren

Aktivieren Sie ADB für die gewünschten Cloud-Telefon-Profile im Profilbereich.

![ADB aktivieren](../img/enable-adb.webp)

Sie können ADB auch für mehrere Profile gleichzeitig aktivieren.
![ADB für mehrere Profile aktivieren](../img/enable-adb-multiple.webp)

## Profil starten

Nachdem Sie das Profil geöffnet haben, klicken Sie auf das 🔗-Symbol, um die IP-Adresse, den Port und den Verbindungscode des Cloud-Telefons zu erhalten.
![IP-Adresse abrufen](../img/get-ip-address.webp)

## Methode 1: Schnellverbindung über Cloud-Telefon-Liste (empfohlen)

TikMatrix unterstützt jetzt das Speichern von Cloud-Telefon-Verbindungsinformationen für eine schnelle Ein-Klick-Verbindung.

### Cloud-Telefon-Informationen speichern

1. Öffnen Sie TikMatrix und klicken Sie auf die Schaltfläche `TCP-Geräte scannen`.
2. Wechseln Sie zur Registerkarte `Cloud-Telefon-Liste`.
3. Klicken Sie auf die Schaltfläche `Hinzufügen`.
4. Füllen Sie die Cloud-Telefon-Informationen aus:
   - **Name**: Ein ansprechender Name zur Identifizierung Ihres Cloud-Telefons (z. B. "GeeLark Phone 1")
   - **IP-Adresse**: IP-Adresse von GeeLark (z. B. `1.2.3.4`)
   - **Port**: Portnummer (Standard: `5555`)
   - **Autorisierungscode**: Verbindungscode von GeeLark (z. B. `f850ef`)
5. Klicken Sie auf `Speichern`.

### Ein-Klick-Verbindung

Nach dem Speichern können Sie auf zwei Arten verbinden:

**Einzelverbindung:**

- Klicken Sie auf die 🔌-Schaltfläche neben einem beliebigen Cloud-Telefon in der Liste.

**Massenverbindung:**

- Wählen Sie mehrere Cloud-Telefone über die Kontrollkästchen aus.
- Klicken Sie oben auf die Schaltfläche `Verbinden (X)`.

Das System führt automatisch aus:

1. Verbindung über ADB
2. Ausführung des glogin-Authentifizierungsbefehls
3. Anzeige der Verbindungsergebnisse

Nach erfolgreicher Verbindung aktualisieren Sie die Geräteliste, um Ihre Cloud-Telefone zu sehen.

## Methode 2: Manuelle Verbindung über ADB-Terminal

Wenn Sie eine manuelle Verbindung bevorzugen oder Fehler beheben müssen:

### ADB-Befehle ausführen

1. Klicken Sie in TikMatrix auf `Einstellungen` > `ADB-Terminal öffnen`.
2. Führen Sie den Verbindungsbefehl je nach Betriebssystem aus und überprüfen Sie, ob Ihr Telefon in der Liste mit dem Status `device` erscheint.
3. Führen Sie den Authentifizierungsbefehl aus, um die Verbindung zu bestätigen.

### Für Windows-Systeme

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### Für Mac/Linux-Systeme

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> Zum Beispiel, wenn Ihre IP-Adresse `1.2.3.4`, Port `5555` und Verbindungscode `f850ef` ist
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## Tipps

- Speichern Sie alle Cloud-Telefone in der Liste für schnellen Zugriff.
- Der Verbindungsstatus wird neben jedem Cloud-Telefon angezeigt.
- Sie können gespeicherte Cloud-Telefone jederzeit bearbeiten oder löschen.
- Verbindungsinformationen werden lokal auf Ihrem Computer gespeichert.
