---
sidebar_position: 2
---

# Conectar ao CloudPhone

Este guia ajudará você a conectar-se a um GeeLark CloudPhone usando o TikMatrix.

:::warning Recomendação de Rede
Certifique-se de que seu computador e o data center do CloudPhone tenham largura de banda de rede suficiente e estável. Para melhor confiabilidade das tarefas automatizadas, é recomendado que seu computador e o data center do cloud phone estejam localizados no mesmo país ou região para reduzir latência e perda de pacotes.
:::

Você pode adquirir um CloudPhone da [GeeLark](https://www.geelark.cn?invite_code=XHY6a8).

## Habilitar ADB

Habilite ADB no perfil desejado do CloudPhone na seção de Perfis.

![Enable ADB](../img/enable-adb.webp)

Você também pode habilitar ADB para múltiplos perfis de uma vez.
![Enable ADB for Multiple Profiles](../img/enable-adb-multiple.webp)

## Iniciar o Perfil

Após abrir o perfil, clique no ícone 🔗 para obter o endereço IP, porta e código de conexão do CloudPhone.
![Get IP Address](../img/get-ip-address.webp)

## Método 1: Conexão Rápida com Lista de Cloud Phones (Recomendado)

O TikMatrix agora suporta salvar informações de conexão de cloud phones para conexão rápida com um clique.

### Salvar Informações do Cloud Phone

1. Abra o TikMatrix e clique no botão `Scan TCP Devices`.
2. Mude para a aba `Cloud Phone List`.
3. Clique no botão `Add`.
4. Preencha as informações do cloud phone:
   - **Name**: Um nome amigável para identificar seu cloud phone (ex.: "GeeLark Phone 1")
   - **IP Address**: O endereço IP do GeeLark (ex.: `1.2.3.4`)
   - **Port**: O número da porta (padrão: `5555`)
   - **Auth Code**: O código de conexão do GeeLark (ex.: `f850ef`)
5. Clique em `Save`.

### Conectar ao Cloud Phone

Uma vez salvo, você pode conectar de duas maneiras:

**Conexão Única:**

- Clique no botão 🔌 ao lado de qualquer cloud phone na lista.

**Conexão em Lote:**

- Selecione múltiplos cloud phones usando as caixas de seleção.
- Clique no botão `Connect (X)` no topo.

O sistema automaticamente:

1. Conectará via ADB
2. Executará o comando de autenticação glogin
3. Mostrará os resultados da conexão

Após conexão bem-sucedida, atualize a lista de dispositivos para ver seus cloud phones.

## Método 2: Conexão Manual via Terminal ADB

Se você preferir conexão manual ou precisar solucionar problemas:

### Executar Comandos ADB

1. Clique em `Settings` > `Open ADB Terminal` no TikMatrix.
2. Execute o comando de conexão baseado no seu sistema operacional e verifique se seu telefone aparece na lista com status `device`.
3. Execute o comando de autenticação para autenticar a conexão.

### Para sistemas Windows

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### Para sistemas Mac/Linux

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> Por exemplo, se seu endereço IP é `1.2.3.4` e sua porta é `5555` e o código de conexão é `f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## Dicas

- Salve todos os seus cloud phones na lista para acesso rápido.
- O status da conexão é exibido ao lado de cada cloud phone.
- Você pode editar ou excluir cloud phones salvos a qualquer momento.
- As informações de conexão são armazenadas localmente no seu computador.

## Etapas Finais

Abra o TikMatrix e aguarde o CloudPhone aparecer na lista de dispositivos. Se não aparecer imediatamente, tente atualizar a lista de dispositivos.

Parabéns! Você está agora conectado ao seu CloudPhone e pode gerenciá-lo como qualquer outro dispositivo Android no TikMatrix.
