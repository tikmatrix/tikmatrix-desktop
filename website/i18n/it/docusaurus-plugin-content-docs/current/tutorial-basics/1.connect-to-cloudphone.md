---
sidebar_position: 2
---

# ConnessioneCloud Phone

本Guida将Aiuto你UtilizzoTikMatrixConnessione到GeeLarkCloud Phone。

:::warning ReteSuggerimento
请尽量保证你的电脑与Cloud Phone所在机房之间的Rete带宽充足且稳定。为获得最佳效果，Consigliato将电脑与Cloud Phone机房放在同一国家或同一地区，以降低延迟和丢包，从而保证Automatico化Attività稳定可靠Esegui。
:::

你可以从[GeeLark](https://www.geelark.cn?invite_code=XHY6a8)购买云手机。

## AbilitaADB

在Configurazione文件部分为所需的Cloud PhoneConfigurazione文件AbilitaADB。

![启用ADB](../img/enable-adb.webp)

你也可以同时为多个Configurazione文件AbilitaADB。
![为多个配置文件启用ADB](../img/enable-adb-multiple.webp)

## AvviaConfigurazione文件

ApriConfigurazione文件后，点击🔗图标获取Cloud Phone的Indirizzo IP、Porta和Connessione代码。
![获取IP地址](../img/get-ip-address.webp)

## 方法一：UtilizzoCloud Phone列表快速Connessione（Consigliati）

TikMatrix现在SupportoSalvaCloud PhoneConnessione信息，实现一键快速Connessione。

### SalvaCloud Phone信息

1. ApriTikMatrix，点击`扫描TCP设备`按钮。
2. 切换到`云手机列表`Tag页。
3. 点击`添加`按钮。
4. 填写Cloud Phone信息：
   - **名称**：为Cloud Phone起一个便于识别的名称（例如："GeeLarkTelefono1"）
   - **Indirizzo IP**：从GeeLark获取的Indirizzo IP（例如：`1.2.3.4`）
   - **Porta**：Porta号（Predefinito：`5555`）
   - **Connessione码**：从GeeLark获取的Connessione代码（例如：`f850ef`）
5. 点击`保存`。

### 一键Connessione

Salva后，你可以通过以下两种方式Connessione：

**单个Connessione：**

- 点击Cloud Phone列表中任意Cloud Phone旁边的🔌按钮。

**In MassaConnessione：**

- Utilizzo复选框选择多个Cloud Phone。
- 点击顶部的`连接 (X)`按钮。

系统会AutomaticoCompletato以下Operazioni：

1. 通过ADBConnessione
2. Eseguiglogin认证命令
3. MostraConnessione结果

ConnessioneSuccesso后，AggiornaDispositivi列表即可看到你的Cloud Phone。

## 方法二：通过ADB终端ManualeConnessione

如果你更Mi PiaceManualeConnessione或需要排查问题：

### EseguiADB命令

1. 在TikMatrix中点击`设置` > `打开ADB终端`。
2. 根据你的Operazioni系统Esegui相应的Connessione命令，并检查你的TelefonoVuoi出现在列表中，状态为`device`。
3. Esegui认证命令来验证Connessione。

### Windows系统

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### Mac/Linux系统

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> 例如，如果你的Indirizzo IPSì`1.2.3.4`，PortaSì`5555`，Connessione代码Sì`f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## Suggerimento

- 将TuttiCloud PhoneSalva到列表中以便快速访问。
- Ogni个Cloud Phone旁边会MostraConnessione状态。
- 你可以随时Modifica或Elimina已Salva的Cloud Phone。
- Connessione信息存储在你的本地电脑上。
