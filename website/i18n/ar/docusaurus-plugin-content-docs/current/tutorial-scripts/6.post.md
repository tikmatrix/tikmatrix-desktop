---
sidebar_position: 6
---

# نشر المنشور

يُستخدم سكريبت نشر المنشور لنشر مقاطع الفيديو أو الصور على TikTok.

## الخطوات

1. **تحضير المحتوى**:
   - الخيار 1: انقر على زر `مكتبة المحتوى` لتحميل مقاطع الفيديو/الصور إلى مكتبة المحتوى.
   - الخيار 2: استخدم مجلدًا محليًا كمصدر للمحتوى، قم بتعيين مسار المجلد في تكوين السكريبت.
2. اختر الأجهزة لتشغيل السكريبت.
3. انقر على `السكريبتات` > `نشر المنشور`.
4. قم بتكوين إعدادات المهمة:
    - **مصدر المحتوى**: اختر مكتبة المحتوى أو المجلد المحلي.
    - **مسار المجلد المحلي**: إذا كنت تستخدم مجلدًا محليًا، حدد المسار الذي يحتوي على مقاطع الفيديو/الصور.
    - **طريقة النشر**: اختر طريقة النشر: مشاركة النظام أو زر الإضافة (`+`) أو استخدام الصوت من خلال البحث.
    - **اسم الصوت**: عند استخدام طريقة النشر "استخدام الصوت من خلال البحث"، يمكنك اختيار:
      - إدخال اسم الصوت للبحث (على سبيل المثال: "original sound"، "trending music")
      - إدخال رابط صوت TikTok مباشرةً (على سبيل المثال: `https://www.tiktok.com/t/ZP8hvBwCb/`)
    - **نوع المحتوى**: اختر فيديو أو صورة.
    - **إضافة الصوت**: تمكين/تعطيل إضافة الصوت.
    - **وقت الانتظار بعد التحميل**: تعيين وقت التأخير بعد التحميل (5-60 ثانية)، للتأكد من أن خادم TikTok يعالج المحتوى بالكامل قبل المتابعة.
    - **إضافة رابط المنتج**: تمكين/تعطيل إضافة رابط المنتج.
    - **تنسيق التعليق التوضيحي**: اختر أحد تنسيقات التعليق التوضيحي:
      - **وضع الأسطر المتعددة**: كل سطر عبارة عن تعليق توضيحي مستقل، يستخدم النظام التعليقات بالتسلسل بشكل دائري.
      - **وضع Spintax**: استخدم بناء جملة `{خيار1|خيار2}` لإنشاء نص عشوائي.
    - **العنوان**: أدخل التعليق التوضيحي وفقًا للتنسيق المحدد. يدعم الإشارة إلى المستخدمين (مثل @TikMatrixPhoneFarm) والهاشتاغ (مثل #tag1 #tag2 #tag3). انظر أدناه للحصول على شرح مفصل للتنسيق.
    - **علامات المحتوى**: تصفية المحتوى حسب العلامة. إذا لم يتم تعيينها، استخدم جميع المحتويات.
5. انقر على `بدء السكريبت` لبدء التنفيذ.

## شرح تنسيق التعليق التوضيحي

يدعم النظام تنسيقين للتعليق التوضيحي، مما يتيح لك التحكم المرن في عناوين المنشورات.

### وضع الأسطر المتعددة (افتراضي)

**كيفية العمل:**
- كل سطر يمثل تعليقًا توضيحيًا مستقلاً
- يستخدم النظام التعليقات بالتسلسل ويكررها
- استخدم `###` داخل السطر لإضافة سطر جديد

**مثال:**

```
التعليق الأول###يدعم السطر الجديد
التعليق الثاني###يدعم أيضًا السطر الجديد
التعليق الثالث بدون سطر جديد
```

**الاختلافات بين المنصات:**
- **TikTok**: استخدم `##` لفصل العنوان والوصف (على سبيل المثال: `العنوان##الوصف الطويل`)
- **Instagram**: لا يدعم `##`، سيظهر تحذير

### وضع Spintax

**كيفية العمل:**
- قالب تعليق توضيحي واحد فقط
- استخدم بناء جملة `{خيار1|خيار2|خيار3}` لتحقيق التنوع العشوائي
- استخدم **أسطر جديدة أصلية** (اضغط على Enter) لتحقيق تعليق توضيحي متعدد الأسطر
- لا تستخدم `###` - لن يتم تحليله

**مثال:**

```
{اليوم|غدًا|بعد غد} الطقس {جميل|رائع|ممتاز}
مرحبًا بكم في متابعة حسابي##لمعرفة المزيد انقر على الرابط
```

**الاختلافات بين المنصات:**
- **TikTok**: يمكن استخدام `##` لفصل العنوان والوصف
- **Instagram**: لا يدعم `##`

**ملاحظة:** في وضع Spintax، إذا تم اكتشاف `###`، سينبهك النظام لاستخدام أسطر جديدة أصلية.

## إدارة المحتوى

### أوضاع استخدام الموسيقى

عندما يتم تعيين **طريقة النشر** على "استخدام الصوت من خلال البحث"، يدعم السكريبت وضعين مختلفين لإضافة الموسيقى:

#### 1. وضع البحث عن الصوت

أدخل اسم الصوت للبحث عن الموسيقى داخل TikTok:

- أمثلة: `original sound`، `trending music`، `viral beat`
- سيبحث السكريبت في مكتبة موسيقى TikTok ويختار أول نتيجة مطابقة
- هذا الوضع مناسب لاستخدام الأصوات الشائعة أو الرائجة

#### 2. وضع رابط الصوت

أدخل رابط صوت TikTok المباشر لاستخدام موسيقى محددة:

- تنسيقات الروابط المدعومة:
  - رابط قصير: `https://www.tiktok.com/t/ZP8hvBwCb/`
  - صفحة الموسيقى: `https://www.tiktok.com/music/original-sound-7473897776619834646`
  - رابط VM: `https://vm.tiktok.com/ZMhvBwCbD/`
- يكتشف السكريبت تلقائيًا الروابط التي تبدأ بـ `https://` ويفتحها مباشرةً
- هذا الوضع مناسب لاستخدام صوت محدد من فيديو معين

### مكتبة المحتوى

- **علامات المحتوى**: أضف علامات للمحتوى لتصفيتها عند النشر. إذا لم يتم تحديد علامة، استخدم جميع المحتويات.
- **عنوان المحتوى**: يمكنك تعيين عنوان مخصص لكل محتوى. إذا تم تعيينه، سيكون له الأولوية على إعدادات النشر.
- **اسم مستخدم المحتوى**: يمكنك تعيين اسم مستخدم مخصص لكل محتوى. سيتم استخدام المحتوى الذي يطابق اسم المستخدم بالأولوية.
- **ترتيب المحتوى**: قم بتعيين رقم ترتيب للمحتوى (مفيد للمنشورات متعددة الصور؛ كلما كان الرقم أصغر، كان أولى).

### مصدر المجلد المحلي

- **بنية المجلد**:

  ```text
  /مسار-المحتوى/
  ├── caption.txt (اختياري - المجلد الجذر كاحتياطي)
  ├── sound_names.txt (اختياري - المجلد الجذر كاحتياطي)  
  ├── sound_urls.txt (اختياري - المجلد الجذر كاحتياطي)
  ├── @اسم_المستخدم1/
  │   ├── video1.mp4
  │   ├── video2.mp4
  │   ├── image1.jpg
  │   ├── image2.jpg
  │   ├── caption.txt (اختياري)
  │   ├── sound_names.txt (اختياري)
  │   └── sound_urls.txt (اختياري)
  ├── @اسم_المستخدم2/
  │   ├── content1.mp4
  │   ├── content2.jpg
  │   ├── caption.txt (اختياري)
  │   ├── sound_names.txt (اختياري)
  │   └── sound_urls.txt (اختياري)
  └── ...
  ```

- **مسار المجلد**: حدد المسار الجذر الذي يحتوي على مجلدات فرعية لاسم المستخدم.
- **مجلدات فرعية لاسم المستخدم**: يجب أن يكون لكل اسم مستخدم مجلده الفرعي الخاص (على سبيل المثال، `@اسم_المستخدم1`، `@اسم_المستخدم2`).
- **المجلد الجذر كاحتياطي**: إذا لم يكن مجلد اسم المستخدم موجودًا أو كان اسم المستخدم فارغًا، سيتم قراءة المحتوى والإعدادات من المجلد الجذر.
- **التنسيقات المدعومة**:
  - الفيديو: MP4
  - الصورة: JPG, PNG
- **ملف العنوان**: ملف `caption.txt` اختياري في كل مجلد لاسم المستخدم للعناوين المخصصة. إذا لم يتم العثور عليه، سيعود إلى `caption.txt` في المجلد الجذر.
  - **شرح التنسيق**: يدعم Caption.txt فقط تنسيق قالب Spintax (نفس استخدام وضع Spintax في الواجهة):
    - قم بتكوين قالب Spintax، واستخدم بناء جملة `{خيار1|خيار2}` لتحقيق التنوع العشوائي
    - استخدم أسطر جديدة أصلية (مفتاح Enter) لتحقيق أسطر متعددة
    - يدعم فاصل `##` للعنوان/الوصف (TikTok فقط، Instagram لا يدعم)
  - **ملاحظة**: Caption.txt لا يدعم وضع الاختيار التسلسلي متعدد الأسطر
- **ملفات الموسيقى**:
  - **sound_names.txt**: يحتوي على أسماء الموسيقى المستخدمة للبحث، واحد في كل سطر. يتم قراءتها بالتسلسل، وعند الوصول إلى النهاية تعود إلى البداية.
  - **sound_urls.txt**: يحتوي على روابط موسيقى TikTok، واحد في كل سطر. يتم قراءتها بالتسلسل، وعند الوصول إلى النهاية تعود إلى البداية.
  - إذا لم يكن مجلد اسم المستخدم موجودًا، اقرأ من ملفات الموسيقى في المجلد الجذر.
- **اختيار الملفات**:
  - للفيديو: اختر فيديو واحد عشوائيًا في كل مرة للنشر
  - للصور: اختر صورًا متعددة بناءً على إعداد عدد الصور
- **تتبع الاستخدام**: سيتم وضع علامة على الملفات تلقائيًا كمستخدمة لتجنب التكرار.

### شرح تنسيق ملف Caption.txt

عند استخدام **مصدر المجلد المحلي**، يُستخدم ملف `caption.txt` لتكوين قالب Spintax. يمكن لهذا الملف تجاوز تكوين العنوان في الواجهة.

**ملاحظة مهمة**: يدعم Caption.txt فقط تنسيق قالب Spintax، ولا يدعم وضع الاختيار التسلسلي متعدد الأسطر.

#### تنسيق قالب Spintax

قم بتكوين قالب Spintax، واستخدم بناء جملة `{خيار1|خيار2}` لإنشاء تنوعات نصية عشوائية. استخدم أسطر جديدة أصلية (اضغط على مفتاح Enter) لتحقيق أسطر متعددة:

```text
{صباح الخير|مرحبًا|أهلاً}! انظر إلى هذا الفيديو {المذهل|الرائع|الممتاز}!
{تابع للمزيد|أعجب وشارك|اذكر أصدقاءك}
```

المخرجات المحتملة:

- "صباح الخير! انظر إلى هذا الفيديو المذهل!"
- "مرحبًا! انظر إلى هذا الفيديو الرائع!"
- "أهلاً! انظر إلى هذا الفيديو الممتاز!"

**تنوعات Spintax المعقدة:**

```text
{🔥|✨|🎉} {لا تفوت|شاهد|انظر} هذا {المذهل|الرائع|الممتاز} {الفيديو|المحتوى|المنشور}!
{تابع للمزيد|أعجب وشارك|اذكر أصدقاءك} {#رائج|#فيرال|#fyp} #tiktok
```

**Spintax مع فاصل العنوان/الوصف لـ TikTok:**

```text
{مثير|مذهل|رائع} {الأخبار|التحديث|الاكتشاف}##هذا ما تحتاج معرفته عن هذا {الممتاز|الرائع|الاستثنائي} {التطور|التحديث|القصة}!
```

#### مثال كامل لـ Caption.txt

مثال كامل لقالب Spintax:

```text
{تحفيز الصباح|الإلهام اليومي|ابدأ يومك}! ��
{أحب|استمتع|مولع بـ} هذا {الجميل|المذهل|الرائع} المنظر! 🌅
{لا أصدق|متحمس لـ|متشوق لـ} {هذه اللحظة|هذه التجربة|هذه المغامرة}! ✨
```

**مثال مع فاصل العنوان/الوصف لـ TikTok:**

```text
{رائع|ممتع|مسلي} محتوى الفيديو##هذا {مذهل|رائع|ممتاز} {فيديو|محتوى|عمل}، مرحبًا بـ{الإعجاب|المتابعة|إعادة النشر}!
```

  **مثال على ملف Sound_names.txt:**

  ```text
  الصوت الأصلي
  موسيقى رائجة
  إيقاع فيرال
  أغنية شائعة
  موسيقى رقص
  ```

  **مثال على ملف Sound_urls.txt:**

  ```text
  https://www.tiktok.com/t/ZP8hvBwCb/
  https://www.tiktok.com/music/original-sound-7473897776619834646
  https://vm.tiktok.com/ZMhvBwCbD/
  https://www.tiktok.com/t/ZP8hvBwCb/
  ```

## الأسئلة الشائعة

1. لماذا تفشل المهمة؟
   - سكريبتات الأتمتة تحاكي سلوك تطبيقات TikTok/Instagram، وقد تفشل في بعض الحالات، مثل مشاكل الشبكة، استجابة التطبيق غير الطبيعية، النوافذ المنبثقة غير المتوقعة، مشاكل أداء الجهاز، مشاكل توافق الجهاز، إلخ.
   - إذا فشلت بعد عدة محاولات، أرسل تذكرة للحصول على المساعدة.
2. كيف يمكن تحقيق التشغيل غير المراقب؟
   - أنشئ مجموعة أجهزة، واستخدم وظيفة الجدولة الزمنية في إعدادات النشر للمجموعة، سيقوم البرنامج تلقائيًا بإنشاء وتشغيل مهام النشر في الوقت المحدد كل يوم.
3. كيف يمكن تحقيق ترتيب منشورات العرض الشرائحي؟
   - في مكتبة المحتوى، قم بتعيين رقم ترتيب لكل محتوى صورة، كلما كان الرقم أصغر، كان أولى. عند النشر، سيختار السكريبت الصور وفقًا لرقم الترتيب.
   - يمكنك أيضًا استخدام أسماء الملفات التي تتوافق مع المواصفات التالية لتعيين رقم الترتيب عند استيراد المحتوى بكميات كبيرة:
     - `sort_1_وصف_الصورة.png`
     - `sort_2_وصف_الصورة.png`
     - `sort_3_وصف_الصورة.png`
4. كيف يمكن استخدام محتوى وعناوين وموسيقى مختلفة لكل حساب؟
   - عند استخدام مجلد محلي، أنشئ مجلدًا فرعيًا لكل اسم مستخدم، وأنشئ ملفات `caption.txt` و `sound_names.txt` أو `sound_urls.txt` في المجلد الفرعي لتحديد العناوين والموسيقى المخصصة. سيستخدم السكريبت بالأولوية الملفات التي تطابق اسم المستخدم.
   - عند استخدام مكتبة المحتوى، قم بتعيين حقل اسم المستخدم وحقل العنوان لكل محتوى، سيستخدم السكريبت بالأولوية المحتوى والعناوين التي تطابق اسم المستخدم.
5. ما هي تنسيقات المحتوى المدعومة؟
   - الفيديو: MP4
   - الصورة: JPG، PNG

6. هل يمكن إعادة استخدام محتوى الصور والفيديو؟

  لا. لحماية حسابك، سيقوم النظام تلقائيًا بوضع علامة على المحتوى كمستخدم بعد نشره، لتجنب إعادة الاختيار. هذا يمكن أن يقلل من خطر النشر المتكرر، ويقلل من احتمالية تقييد الحساب أو حظره.

## المستندات ذات الصلة

- [Permission Denied - Unable to Save Draft Videos](../troubleshooting/4.permission-denied-draft-videos.md)
