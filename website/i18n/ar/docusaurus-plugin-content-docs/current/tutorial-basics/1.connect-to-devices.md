---
sidebar_position: 1
---

# توصيل الأجهزة

يدعم TikMatrix اتصالات USB و TCP لأجهزة Android.

## اتصال USB

1. افتح إعدادات المطور على الهاتف وفعّل تصحيح USB (راجع الصور أدناه).
   - **ملاحظة لمستخدمي هواتف Xiaomi**: يجب أيضًا تفعيل خيارات "التثبيت عبر USB" و "تصحيح USB (إعدادات الأمان)" في خيارات المطور.
2. قم بتوصيل الهاتف بالكمبيوتر عبر USB.
3. عند ظهور الرسالة على الهاتف، انقر على `السماح` لتفعيل تصحيح USB.
4. انتظر حتى يتم إنشاء الاتصال.

## اتصال TCP

1. قم بتوصيل الهاتف بالكمبيوتر عبر USB.
2. في TikMatrix، انقر على `أوامر ADB` > `تفعيل TCP` لتفعيل اتصال TCP/IP.
3. افصل كابل USB (إذا كنت تستخدم صندوق الهاتف، يمكنك التبديل إلى وضع OTG).
4. انقر على زر `مسح أجهزة TCP` وأدخل نطاق IP (يمكنك العثور على هذه المعلومات في صفحة إدارة الموجه).
5. انقر على `مسح` للبحث عن الأجهزة في نطاق IP المحدد.
6. انتظر حتى يتم إنشاء الاتصال.

**لمستخدمي الهواتف السحابية**: يدعم TikMatrix الآن حفظ معلومات اتصال الهواتف السحابية للوصول السريع. قم بالتبديل إلى علامة التبويب `قائمة الهواتف السحابية` في مربع حوار المسح لحفظ وإدارة اتصالات الهواتف السحابية الخاصة بك. للحصول على تعليمات مفصلة، راجع دليل [الاتصال بالهاتف السحابي](./1.connect-to-cloudphone.md).

## لقطات الشاشة

![تفعيل تصحيح USB الخطوات 1-2](../img/usbsetp12.webp)
![تفعيل تصحيح USB الخطوات 3-4](../img/usbsetp34.webp)
![تفعيل تصحيح USB الخطوات 5-6](../img/usbsetp56.webp)

### نصائح

- قد تختلف خطوات التشغيل بين العلامات التجارية المختلفة، ولكن العملية الأساسية متشابهة.
- إذا لم يكن هناك استجابة، حاول استبدال كابل البيانات أو استخدام منفذ USB في الجزء الخلفي من الكمبيوتر.

## كيفية الاتصال باللوحة الأم أو الهيكل

1. اطلب من بائع اللوحة الأم أو الهيكل ملفات التفويض `adbkey` و `adbkey.pub`.
2. انسخ هذه الملفات إلى دليل `C:\Users\اسم_المستخدم\.android` على جهاز الكمبيوتر الخاص بك.
3. أعد تشغيل الكمبيوتر.

## لقطة شاشة لملفات مفاتيح ADB

![ملفات مفاتيح ADB](../img/adbkey.webp)

### نصائح أخرى

- قم بعمل نسخة احتياطية من ملفات التفويض الخاصة بك. إذا أعدت تثبيت النظام أو استبدلت الكمبيوتر، ستحتاج إليها مرة أخرى.
- إذا فقدت هذه الملفات، ستحتاج إلى السماح بالأذونات يدويًا على شاشة الهاتف.

## كيفية توصيل المزيد من أجهزة USB

💡 نظريًا، يمكن لـ USB دعم ما يصل إلى 127 جهازًا.  
ولكن عمليًا، معظم اللوحات الأم الاستهلاكية تحدها عند حوالي 40 جهازًا بسبب قيود البرامج الثابتة ومجموعة الرقائق.

🖥️ اللوحات الأم من فئة الخوادم (مثل معمارية X79) مختلفة — فهي عادةً ما تزيل هذه القيود على USB، مما يسمح لك بالتوسع على نطاق واسع.

🔧 كيفية توصيل المزيد من الأجهزة بنجاح:

1. استخدم منافذ USB في الجزء الخلفي من الكمبيوتر — فهي متصلة بشكل أكثر مباشرة باللوحة الأم.  
2. استخدم منافذ USB 2.0 (عادةً سوداء)، وليس USB 3.0 (زرقاء)، حيث قد تكون الأخيرة غير مستقرة لمجموعات الأجهزة الكبيرة.  
3. في إعدادات BIOS:  
   - قم بتعطيل XHCI  
   - قم بتفعيل EHCI

ستساعد هذه الخطوات نظامك على التعرف على المزيد من الأجهزة بشكل موثوق.
