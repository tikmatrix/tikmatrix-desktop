---
sidebar_position: 2
---

# الاتصال بالهاتف السحابي

سيساعدك هذا الدليل على الاتصال بهواتف GeeLark السحابية باستخدام TikMatrix.

:::warning تنبيه الشبكة
يرجى التأكد من أن عرض النطاق الترددي للشبكة بين جهاز الكمبيوتر الخاص بك ومركز بيانات الهاتف السحابي كافٍ ومستقر. للحصول على أفضل النتائج، يُنصح بوضع الكمبيوتر ومركز بيانات الهاتف السحابي في نفس البلد أو المنطقة لتقليل زمن الانتقال وفقدان الحزم، وبالتالي ضمان التشغيل المستقر والموثوق للمهام الآلية.
:::

يمكنك شراء الهواتف السحابية من [GeeLark](https://www.geelark.cn?invite_code=XHY6a8).

## تفعيل ADB

قم بتفعيل ADB للملف الشخصي المطلوب للهاتف السحابي في قسم الملفات الشخصية.

![تفعيل ADB](../img/enable-adb.webp)

يمكنك أيضًا تفعيل ADB لعدة ملفات شخصية في نفس الوقت.
![تفعيل ADB لعدة ملفات شخصية](../img/enable-adb-multiple.webp)

## بدء الملف الشخصي

بعد فتح الملف الشخصي، انقر على أيقونة 🔗 للحصول على عنوان IP والمنفذ ورمز الاتصال للهاتف السحابي.
![الحصول على عنوان IP](../img/get-ip-address.webp)

## الطريقة الأولى: الاتصال السريع باستخدام قائمة الهواتف السحابية (موصى به)

يدعم TikMatrix الآن حفظ معلومات اتصال الهواتف السحابية لتحقيق اتصال سريع بنقرة واحدة.

### حفظ معلومات الهاتف السحابي

1. افتح TikMatrix، وانقر على زر `مسح أجهزة TCP`.
2. قم بالتبديل إلى علامة التبويب `قائمة الهواتف السحابية`.
3. انقر على زر `إضافة`.
4. املأ معلومات الهاتف السحابي:
   - **الاسم**: أعط الهاتف السحابي اسمًا سهل التعرف عليه (على سبيل المثال: "GeeLark Phone 1")
   - **عنوان IP**: عنوان IP الذي حصلت عليه من GeeLark (على سبيل المثال: `1.2.3.4`)
   - **المنفذ**: رقم المنفذ (افتراضي: `5555`)
   - **رمز الاتصال**: رمز الاتصال الذي حصلت عليه من GeeLark (على سبيل المثال: `f850ef`)
5. انقر على `حفظ`.

### الاتصال بنقرة واحدة

بعد الحفظ، يمكنك الاتصال بإحدى الطريقتين التاليتين:

**الاتصال الفردي:**

- انقر على زر 🔌 بجانب أي هاتف سحابي في القائمة.

**الاتصال الجماعي:**

- استخدم مربعات الاختيار لتحديد عدة هواتف سحابية.
- انقر على زر `الاتصال (X)` في الأعلى.

سيكمل النظام تلقائيًا العمليات التالية:

1. الاتصال عبر ADB
2. تنفيذ أمر مصادقة glogin
3. عرض نتيجة الاتصال

بعد الاتصال بنجاح، قم بتحديث قائمة الأجهزة لرؤية هاتفك السحابي.

## الطريقة الثانية: الاتصال اليدوي عبر طرفية ADB

إذا كنت تفضل الاتصال اليدوي أو تحتاج إلى استكشاف المشكلات:

### تشغيل أوامر ADB

1. في TikMatrix انقر على `الإعدادات` > `فتح طرفية ADB`.
2. قم بتشغيل أمر الاتصال المناسب لنظام التشغيل الخاص بك، وتحقق من ظهور هاتفك في القائمة بحالة `device`.
3. قم بتشغيل أمر المصادقة للتحقق من الاتصال.

### نظام Windows

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### نظام Mac/Linux

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> على سبيل المثال، إذا كان عنوان IP الخاص بك هو `1.2.3.4`، والمنفذ هو `5555`، ورمز الاتصال هو `f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## نصائح

- احفظ جميع الهواتف السحابية في القائمة للوصول السريع.
- سيتم عرض حالة الاتصال بجانب كل هاتف سحابي.
- يمكنك تعديل أو حذف الهواتف السحابية المحفوظة في أي وقت.
- يتم تخزين معلومات الاتصال على جهاز الكمبيوتر المحلي الخاص بك.
