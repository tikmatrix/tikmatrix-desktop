---
sidebar_position: 6
title: تكوين برنامج تدفئة الحساب النصي
description: مرجع التكوين الكامل لبرنامج تدفئة الحساب النصي
---

توثق هذه الصفحة معاملات التكوين لبرنامج `account_warmup` النصي المستخدم في إنشاء المهام.

## نظرة عامة

`account_warmup` يستخدم البرنامج النصي لـتدفئة حسابات TikTok أو Instagram 账号。它会观看فيديو，并根据التكوين的概率随机进行点赞、关注、收藏和评论。يساعد ذلك الحسابات الجديدة في بناء تاريخ تفاعل وتجنب اكتشافها كروبوتات.

## تكوين البرنامج النصي (`script_config`)

`script_config` يحتوي كائنتدفئة الحسابعلى معاملات البرنامج النصي. فيما يلي المعاملات المتاحة:

### المعاملات

| المعاملات | النوع | مطلوب | القيمة الافتراضية | الوصف |
|-----|------|------|-------|------|
| task_duration | رقم | لا | 600 | 预热المدة الإجمالية للمهمة (بالثواني) |
| topic | نص | لا | "" | كلمات مفتاحية للموضوع (كلمة لكل سطر، يتم الاختيار عشوائيًا) |
| min_duration | رقم | لا | 15 | الحد الأدنى لمدة مشاهدة الفيديو (بالثواني) |
| max_duration | رقم | لا | 30 | الحد الأقصى لمدة مشاهدة الفيديو (بالثواني) |
| like_probable | رقم | لا | 0 | احتمالية الإعجاب بالفيديو (0-100) |
| floow_probable | رقم | لا | 0 | احتمالية متابعة منشئ الفيديو (0-100) |
| collect_probable | رقم | لا | 0 | احتمالية حفظ/إضافة الفيديو للمفضلة (0-100) |
| comment_probable | رقم | لا | 0 | احتمالية التعليق على الفيديو (0-100) |
| comment | نص | لا | "" | قوالب التعليقات (واحد لكل سطر، يتم الاختيار عشوائيًا) |
| insert_emoji | منطقي | لا | false | ما إذا كان سيتم إدراج رموز تعبيرية عشوائية في التعليقات |
| comment_order | نص | لا | "random" | 评论选择顺序：`random`（随机）أو `sequential`（顺序） |
| generate_by_chatgpt | منطقي | لا | false | نعملااستخدام ChatGPT 生成评论 |
| chatgpt_settings | كائن | لا | {} | ChatGPT التكوين设置（见下文） |

### ChatGPT 设置结构

عندما `generate_by_chatgpt` 设置为 `true` 时，يمكنك استخدام معامل `chatgpt_settings` 对象التكوين ChatGPT 评论生成：

| المعاملات | النوع | مطلوب | الوصف |
|-----|------|------|------|
| api_key | نص | نعم | 您的 OpenAI API 密钥 |
| model | نص | لا | استخدام的模型（默认："gpt-3.5-turbo"）。الخيارات："gpt-3.5-turbo"、"gpt-4"、"gpt-4-turbo" |
| prompt | نص | لا | 生成评论的自定义تلميح词。默认生成友好、相关的评论 |
| max_tokens | رقم | لا | 响应的最大 token 数（默认：100） |
| temperature | رقم | لا | 创造性等级 0-2（默认：0.7）。数值越高 = 越有创意 |
| base_url | نص | لا | 自定义 API 端点 URL（用于 Azure OpenAI أو兼容的 API） |

`chatgpt_settings` 对象أمثلة：

```json
{
  "api_key": "sk-your-openai-api-key",
  "model": "gpt-3.5-turbo",
  "prompt": "针对这个视频生成一条简短友好的中文评论",
  "max_tokens": 50,
  "temperature": 0.8,
  "base_url": "https://api.openai.com/v1"
}
```

:::tip 建议
对于新账号，建议从低互动概率（5-15%）开始，然后随时间逐渐增加。这模拟了自然用户行为。
:::

## أمثلة

### 基本تدفئة الحساب

仅观看فيديو的简单预热：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 600,
      "min_duration": 10,
      "max_duration": 30
    }
  }'
```

### 带主题بحث的预热

通过بحث特定主题来预热账号：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 900,
      "topic": "搞笑猫咪\n狗狗视频\n宠物合集",
      "min_duration": 15,
      "max_duration": 45
    }
  }'
```

### 带互动的预热

完整预热，包含点赞、关注和评论：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 1200,
      "topic": "美食\n食谱\n烹饪",
      "min_duration": 20,
      "max_duration": 60,
      "like_probable": 30,
      "floow_probable": 10,
      "collect_probable": 5,
      "comment_probable": 15,
      "comment": "太棒了！🔥\n喜欢这个内容！\n太好了！👏\n哇，太厉害了！",
      "insert_emoji": true,
      "comment_order": "random"
    }
  }'
```

### استخدام ChatGPT 评论的预热

استخدام ChatGPT 生成智能评论：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 1800,
      "topic": "科技评测\n数码产品",
      "min_duration": 30,
      "max_duration": 90,
      "like_probable": 25,
      "comment_probable": 20,
      "generate_by_chatgpt": true,
      "chatgpt_settings": {
        "api_key": "your-api-key",
        "model": "gpt-3.5-turbo",
        "prompt": "针对这个视频生成一条简短友好的评论"
      }
    }
  }'
```

### 多设备批量预热

在多个设备上同时运行预热：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_1", "device_2", "device_3"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 600,
      "min_duration": 15,
      "max_duration": 30,
      "like_probable": 20
    },
    "enable_multi_account": true
  }'
```

### 定时预热任务

安排预热在特定时间运行：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "serials": ["device_serial_1"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 1200,
      "topic": "音乐\n舞蹈\n热门",
      "min_duration": 20,
      "max_duration": 40,
      "like_probable": 15,
      "floow_probable": 5
    },
    "start_time": "09:00"
  }'
```

### 按用户名列表预热

为特定账号创建预热任务：

```bash
curl -X POST http://localhost:50809/api/v1/task \
  -H "Content-Type: application/json" \
  -d '{
    "usernames": ["@my_account1", "@my_account2"],
    "script_name": "account_warmup",
    "script_config": {
      "task_duration": 600,
      "min_duration": 15,
      "max_duration": 30,
      "like_probable": 20,
      "floow_probable": 5
    }
  }'
```

## 响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "task_ids": [301, 302, 303],
    "created_count": 3
  }
}
```

## 最佳实践

1. **从低概率开始**：对于新账号，استخدام低互动概率（5-15%），然后在几天/几周内逐渐增加。

2. **استخدام相关主题**：选择与您账号定位相关的主题，以建立相关的互动历史。

3. **变化观看时长**：设置 min_duration 和 max_duration 之间的范围，以模拟自然的观看模式。

4. **适度的任务时长**：每天运行 2-3 次，每次 10-30 分钟的预热会话，而不نعم持续长时间的会话。

5. **استخدام多样化评论**：提供多个评论模板，以避免可能触发垃圾检测的重复模式。

6. **明智地安排时间**：استخدام `start_time` 在目标受众时区的活跃时段运行预热任务。

## انظر أيضًا

- [API إدارة المهام](./task-management.md) - 创建、列出和管理任务
- [النشرتكوين البرنامج النصي](./post-script.md) - التكوينالنشر脚本المعاملات
- [تكوين برنامج المتابعة النصي](./follow-script.md) - التكوين关注脚本المعاملات
- [取消تكوين برنامج المتابعة النصي](./unfollow-script.md) - التكوين取消关注脚本المعاملات
