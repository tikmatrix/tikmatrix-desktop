---
sidebar_position: 2
---

# Połącz z CloudPhone

Ten przewodnik pomoże Ci połączyć się z GeeLark CloudPhone za pomocą TikMatrix.

:::warning Zalecenia dotyczące sieci
Upewnij się, że Twój komputer i centrum danych CloudPhone mają wystarczającą, stabilną przepustowość sieci. Aby zapewnić najlepszą niezawodność automatycznych zadań, zaleca się, aby Twój komputer i centrum danych telefonu chmurowego znajdowały się w tym samym kraju lub regionie, aby zmniejszyć opóźnienia i utratę pakietów.
:::

Możesz kupić CloudPhone od [GeeLark](https://www.geelark.cn?invite_code=XHY6a8).

## Włącz ADB

Włącz ADB w żądanym profilu CloudPhone z sekcji Profiles.

![Włącz ADB](../img/enable-adb.webp)

Możesz również włączyć ADB dla wielu profili jednocześnie.
![Włącz ADB dla wielu profili](../img/enable-adb-multiple.webp)

## Uruchom Profil

Po otwarciu profilu kliknij ikonę 🔗, aby uzyskać adres IP, port i kod połączenia dla CloudPhone.
![Pobierz adres IP](../img/get-ip-address.webp)

## Metoda 1: Szybkie połączenie z listą telefonów chmurowych (zalecane)

TikMatrix obsługuje teraz zapisywanie informacji o połączeniu z telefonem chmurowym w celu szybkiego połączenia jednym kliknięciem.

### Zapisz informacje o telefonie chmurowym

1. Otwórz TikMatrix i kliknij przycisk `Scan TCP Devices`.
2. Przełącz się na kartę `Cloud Phone List`.
3. Kliknij przycisk `Add`.
4. Wypełnij informacje o telefonie chmurowym:
   - **Name**: Przyjazna nazwa identyfikująca telefon chmurowy (np. "GeeLark Phone 1")
   - **IP Address**: Adres IP z GeeLark (np. `1.2.3.4`)
   - **Port**: Numer portu (domyślnie: `5555`)
   - **Auth Code**: Kod połączenia z GeeLark (np. `f850ef`)
5. Kliknij `Save`.

### Połącz się z telefonem chmurowym

Po zapisaniu możesz połączyć się na dwa sposoby:

**Pojedyncze połączenie:**

- Kliknij przycisk 🔌 obok dowolnego telefonu chmurowego na liście.

**Połączenie wsadowe:**

- Wybierz wiele telefonów chmurowych za pomocą pól wyboru.
- Kliknij przycisk `Connect (X)` u góry.

System automatycznie:

1. Połączy się przez ADB
2. Wykona polecenie uwierzytelniania glogin
3. Pokaże wyniki połączenia

Po pomyślnym połączeniu odśwież listę urządzeń, aby zobaczyć swoje telefony chmurowe.

## Metoda 2: Ręczne połączenie przez terminal ADB

Jeśli wolisz ręczne połączenie lub musisz rozwiązać problemy:

### Uruchom polecenia ADB

1. Kliknij `Settings` > `Open ADB Terminal` w TikMatrix.
2. Uruchom polecenie połączenia w zależności od systemu operacyjnego i sprawdź, czy telefon pojawia się na liście ze statusem `device`.
3. Uruchom polecenie uwierzytelniania, aby uwierzytelnić połączenie.

### Dla systemów Windows

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### Dla systemów Mac/Linux

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> Na przykład, jeśli Twój adres IP to `1.2.3.4`, port to `5555`, a kod połączenia to `f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## Wskazówki

- Zapisz wszystkie swoje telefony chmurowe na liście, aby uzyskać szybki dostęp.
- Status połączenia jest wyświetlany obok każdego telefonu chmurowego.
- Możesz w każdej chwili edytować lub usunąć zapisane telefony chmurowe.
- Informacje o połączeniu są przechowywane lokalnie na Twoim komputerze.

## Ostatnie kroki

Otwórz TikMatrix i poczekaj, aż CloudPhone pojawi się na liście urządzeń. Jeśli nie pojawi się natychmiast, spróbuj odświeżyć listę urządzeń.

Gratulacje! Jesteś teraz połączony z CloudPhone i możesz nim zarządzać jak każdym innym urządzeniem Android w TikMatrix.
