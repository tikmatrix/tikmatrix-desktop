---
sidebar_position: 2
---

# เชื่อมต่อกับ CloudPhone

คู่มือนี้จะช่วยคุณเชื่อมต่อกับ GeeLark CloudPhone โดยใช้ TikMatrix

:::warning คำแนะนำเกี่ยวกับเครือข่าย
โปรดตรวจสอบให้แน่ใจว่าคอมพิวเตอร์และศูนย์ข้อมูล CloudPhone ของคุณมีแบนด์วิดท์เครือข่ายที่เพียงพอและเสถียร เพื่อความน่าเชื่อถือที่ดีที่สุดของงานอัตโนมัติ แนะนำให้คอมพิวเตอร์และศูนย์ข้อมูล cloud phone อยู่ในประเทศหรือภูมิภาคเดียวกันเพื่อลดความล่าช้าและการสูญเสียแพ็กเก็ต
:::

คุณสามารถซื้อ CloudPhone ได้จาก [GeeLark](https://www.geelark.cn?invite_code=XHY6a8)

## เปิดใช้งาน ADB

เปิดใช้งาน ADB ในโปรไฟล์ CloudPhone ที่ต้องการจากส่วน Profiles

![Enable ADB](../img/enable-adb.webp)

คุณยังสามารถเปิดใช้งาน ADB สำหรับหลายโปรไฟล์พร้อมกันได้
![Enable ADB for Multiple Profiles](../img/enable-adb-multiple.webp)

## เริ่มโปรไฟล์

หลังจากเปิดโปรไฟล์ คลิกไอคอน 🔗 เพื่อรับที่อยู่ IP พอร์ต และรหัสการเชื่อมต่อสำหรับ CloudPhone
![Get IP Address](../img/get-ip-address.webp)

## วิธีที่ 1: เชื่อมต่ออย่างรวดเร็วด้วย Cloud Phone List (แนะนำ)

TikMatrix ขณะนี้รองรับการบันทึกข้อมูลการเชื่อมต่อ cloud phone เพื่อการเชื่อมต่อคลิกเดียวที่รวดเร็ว

### บันทึกข้อมูล Cloud Phone

1. เปิด TikMatrix และคลิกปุ่ม `Scan TCP Devices`
2. สลับไปที่แท็บ `Cloud Phone List`
3. คลิกปุ่ม `Add`
4. กรอกข้อมูล cloud phone:
   - **Name**: ชื่อที่เป็นมิตรเพื่อระบุ cloud phone ของคุณ (เช่น "GeeLark Phone 1")
   - **IP Address**: ที่อยู่ IP จาก GeeLark (เช่น `1.2.3.4`)
   - **Port**: หมายเลขพอร์ต (ค่าเริ่มต้น: `5555`)
   - **Auth Code**: รหัสการเชื่อมต่อจาก GeeLark (เช่น `f850ef`)
5. คลิก `Save`

### เชื่อมต่อกับ Cloud Phone

เมื่อบันทึกแล้ว คุณสามารถเชื่อมต่อได้สองวิธี:

**การเชื่อมต่อเดี่ยว:**

- คลิกปุ่ม 🔌 ถัดจาก cloud phone ใดๆ ในรายการ

**การเชื่อมต่อแบบกลุ่ม:**

- เลือก cloud phone หลายเครื่องโดยใช้ช่องทำเครื่องหมาย
- คลิกปุ่ม `Connect (X)` ที่ด้านบน

ระบบจะทำโดยอัตโนมัติ:

1. เชื่อมต่อผ่าน ADB
2. รันคำสั่งการยืนยันตัวตน glogin
3. แสดงผลลัพธ์การเชื่อมต่อ

หลังจากเชื่อมต่อสำเร็จ รีเฟรชรายการอุปกรณ์เพื่อดู cloud phone ของคุณ

## วิธีที่ 2: การเชื่อมต่อด้วยตนเองผ่าน ADB Terminal

หากคุณต้องการเชื่อมต่อด้วยตนเองหรือต้องการแก้ไขปัญหา:

### รันคำสั่ง ADB

1. คลิก `Settings` > `Open ADB Terminal` ใน TikMatrix
2. รันคำสั่งการเชื่อมต่อตามระบบปฏิบัติการของคุณและตรวจสอบว่าโทรศัพท์ของคุณปรากฏในรายการพร้อมสถานะ `device`
3. รันคำสั่งการยืนยันตัวตนเพื่อยืนยันการเชื่อมต่อ

### สำหรับระบบ Windows

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### สำหรับระบบ Mac/Linux

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> ตัวอย่างเช่น ถ้าที่อยู่ IP ของคุณคือ `1.2.3.4` และพอร์ตคือ `5555` และรหัสการเชื่อมต่อคือ `f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## เคล็ดลับ

- บันทึก cloud phone ทั้งหมดของคุณในรายการเพื่อการเข้าถึงที่รวดเร็ว
- สถานะการเชื่อมต่อจะแสดงถัดจาก cloud phone แต่ละเครื่อง
- คุณสามารถแก้ไขหรือลบ cloud phone ที่บันทึกไว้ได้ตลอดเวลา
- ข้อมูลการเชื่อมต่อจะถูกเก็บไว้ในเครื่องคอมพิวเตอร์ของคุณ

## ขั้นตอนสุดท้าย

เปิด TikMatrix และรอให้ CloudPhone ปรากฏในรายการอุปกรณ์ หากไม่แสดงทันที ลองรีเฟรชรายการอุปกรณ์

ยินดีด้วย! ตอนนี้คุณเชื่อมต่อกับ CloudPhone ของคุณแล้วและสามารถจัดการได้เหมือนอุปกรณ์ Android อื่นๆ ใน TikMatrix
