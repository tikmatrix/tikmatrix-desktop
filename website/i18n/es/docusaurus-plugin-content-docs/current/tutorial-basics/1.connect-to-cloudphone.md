---
sidebar_position: 2
---

# Conectar a teléfono en la nube

Esta guía te ayudará a conectarte a teléfonos en la nube GeeLark usando TikMatrix.

:::warning Aviso de red
Asegúrate de que el ancho de banda de red entre tu ordenador y el centro de datos del teléfono en la nube sea suficiente y estable. Para obtener mejores resultados, se recomienda colocar el ordenador y el centro de datos del teléfono en la nube en el mismo país o región para reducir la latencia y la pérdida de paquetes, garantizando una ejecución confiable de tareas automatizadas.
:::

Puedes comprar teléfonos en la nube desde [GeeLark](https://www.geelark.cn?invite_code=XHY6a8).

## Habilitar ADB

Habilita ADB para el perfil de teléfono en la nube deseado en la sección de perfiles.

![Habilitar ADB](../img/enable-adb.webp)

También puedes habilitar ADB para múltiples perfiles simultáneamente.
![Habilitar ADB para múltiples perfiles](../img/enable-adb-multiple.webp)

## Iniciar perfil

Después de abrir el perfil, haz clic en el icono 🔗 para obtener la dirección IP, puerto y código de conexión del teléfono en la nube.
![Obtener dirección IP](../img/get-ip-address.webp)

## Método 1: Conexión rápida usando lista de teléfonos en la nube (Recomendado)

TikMatrix ahora admite guardar información de conexión de teléfonos en la nube para una conexión rápida con un clic.

### Guardar información del teléfono en la nube

1. Abre TikMatrix y haz clic en el botón `Escanear dispositivos TCP`.
2. Cambia a la pestaña `Lista de teléfonos en la nube`.
3. Haz clic en el botón `Agregar`.
4. Completa la información del teléfono en la nube:
   - **Nombre**: Da un nombre fácilmente identificable al teléfono en la nube (por ejemplo: "GeeLark teléfono 1")
   - **Dirección IP**: Dirección IP obtenida de GeeLark (por ejemplo: `1.2.3.4`)
   - **Puerto**: Número de puerto (predeterminado: `5555`)
   - **Código de conexión**: Código de conexión obtenido de GeeLark (por ejemplo: `f850ef`)
5. Haz clic en `Guardar`.

### Conexión con un clic

Después de guardar, puedes conectarte de dos maneras:

**Conexión individual:**

- Haz clic en el botón 🔌 junto a cualquier teléfono en la nube en la lista.

**Conexión por lotes:**

- Usa las casillas de verificación para seleccionar múltiples teléfonos en la nube.
- Haz clic en el botón superior `Conectar (X)`.

El sistema completará automáticamente las siguientes operaciones:

1. Conectar vía ADB
2. Ejecutar comando de autenticación glogin
3. Mostrar resultado de conexión

Después de una conexión exitosa, actualiza la lista de dispositivos para ver tu teléfono en la nube.

## Método 2: Conexión manual a través del terminal ADB

Si prefieres conectarte manualmente o necesitas solucionar problemas:

### Ejecutar comandos ADB

1. En TikMatrix, haz clic en `Configuración` > `Abrir terminal ADB`.
2. Ejecuta los comandos de conexión correspondientes según tu sistema operativo y verifica que tu teléfono aparezca en la lista con el estado `device`.
3. Ejecuta el comando de autenticación para verificar la conexión.

### Sistema Windows

```shell
.\adb.exe connect <IP_ADDRESS>:<PORT>
.\adb.exe -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

### Sistema Mac/Linux

```shell
./adb connect <IP_ADDRESS>:<PORT>
./adb -s <IP_ADDRESS>:<PORT> shell glogin <CONNECTION_CODE>
```

> Por ejemplo, si tu dirección IP es `1.2.3.4`, el puerto es `5555` y el código de conexión es `f850ef`
>
> **Windows:**
>
> ```shell
> .\adb.exe connect 1.2.3.4:5555
> .\adb.exe -s 1.2.3.4:5555 shell glogin f850ef
> ```
>
> **Mac/Linux:**
>
> ```shell
> ./adb connect 1.2.3.4:5555
> ./adb -s 1.2.3.4:5555 shell glogin f850ef
> ```

## Consejos

- Guarda todos los teléfonos en la nube en la lista para un acceso rápido.
- El estado de conexión se muestra junto a cada teléfono en la nube.
- Puedes editar o eliminar teléfonos en la nube guardados en cualquier momento.
- La información de conexión se almacena en tu ordenador local.
