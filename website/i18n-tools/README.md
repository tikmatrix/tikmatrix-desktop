# TikMatrix Desktop - 国际化检测工具

这个目录包含 TikMatrix Desktop 网站（Docusaurus）的国际化检测和维护工具。

## 工具说明

### 1. 检查翻译完整性 (`check-i18n.js`)

检查所有 21 种语言的 Docusaurus 翻译文件（`code.json`）：

```bash
cd website/i18n-tools
npm install
npm run check
```

此脚本会检查：

- ✅ **键完整性** - 所有语言文件是否包含相同的翻译键
- ✅ **翻译完整性** - 是否有缺失或空值的翻译
- ✅ **英文后备检测** - 检测是否使用了英文后备值（未真正翻译）
- ✅ **语言检测** - 使用 AI 语言检测检查翻译是否为目标语言

报告包含：

- 每种语言的翻译完成度百分比
- 缺失的翻译键列表
- 可能未翻译的内容（与英文相同）
- 错误的语言检测（如日语内容出现在中文文件中）

### 2. 检查文档完整性 (`check-docs.js`)

检查所有 21 种语言的文档目录是否完整：

```bash
cd website/i18n-tools
npm run check:docs
```

此脚本会检查：

- 📁 所有语言目录是否存在预期的文档文件
- 📄 文档文件是否为空或太小
- 📊 每种语言的文档完整度

Docusaurus 文档结构：

```
docs/                           # 英文文档（默认语言）
├── intro.md
├── roadmap.md
├── tutorial-basics/
└── tutorial-scripts/

i18n/{language}/docusaurus-plugin-content-docs/current/  # 其他语言文档
├── intro.md
├── roadmap.md
├── tutorial-basics/
└── tutorial-scripts/
```

### 3. 同时检查所有内容

```bash
cd website/i18n-tools
npm run check:all
```

这将依次运行翻译检查和文档检查。

## 支持的语言 (21种)

基于 `docusaurus.config.js` 中的配置：

| 语言代码 | 语言名称 | ISO 639-3 | 目录 |
|---------|---------|-----------|------|
| `en` | English | eng | docs/ (根目录) |
| `zh-Hans` | 简体中文 | cmn | i18n/zh-Hans/ |
| `ru` | Русский | rus | i18n/ru/ |
| `ja` | 日本語 | jpn | i18n/ja/ |
| `ko` | 한국어 | kor | i18n/ko/ |
| `es` | Español | spa | i18n/es/ |
| `pt` | Português | por | i18n/pt/ |
| `fr` | Français | fra | i18n/fr/ |
| `de` | Deutsch | deu | i18n/de/ |
| `it` | Italiano | ita | i18n/it/ |
| `ar` | العربية | arb | i18n/ar/ |
| `hi` | हिन्दी | hin | i18n/hi/ |
| `id` | Bahasa Indonesia | ind | i18n/id/ |
| `th` | ไทย | tha | i18n/th/ |
| `vi` | Tiếng Việt | vie | i18n/vi/ |
| `tr` | Türkçe | tur | i18n/tr/ |
| `pl` | Polski | pol | i18n/pl/ |
| `nl` | Nederlands | nld | i18n/nl/ |
| `sv` | Svenska | swe | i18n/sv/ |
| `he` | עברית | heb | i18n/he/ |
| `uk` | Українська | ukr | i18n/uk/ |

## Docusaurus 国际化结构

Docusaurus 使用特定的目录结构来组织翻译：

```
website/
├── docs/                    # 默认语言（英文）的文档
├── i18n/                    # 国际化文件
│   ├── zh-Hans/            # 中文翻译
│   │   ├── code.json       # UI 翻译（导航、按钮等）
│   │   ├── docusaurus-plugin-content-docs/
│   │   │   └── current/    # 文档翻译
│   │   ├── docusaurus-plugin-content-blog/
│   │   │   └── ...         # 博客翻译
│   │   └── docusaurus-theme-classic/
│   │       └── ...         # 主题翻译
│   └── ru/                 # 其他语言...
└── docusaurus.config.js    # 包含语言配置
```

## 与 Video Magic 的区别

本工具基于 video-magic 项目的 `src/i18n` 检测工具，主要区别：

1. **文档结构**：
   - Video Magic: `docs/{language}/` 结构
   - TikMatrix Desktop: Docusaurus 的 `i18n/{language}/docusaurus-plugin-content-docs/current/` 结构

2. **翻译文件格式**：
   - Video Magic: JavaScript ES 模块 (`export default { ... }`)
   - TikMatrix Desktop: JSON 格式 (`code.json`)

3. **翻译文件组织**：
   - Video Magic: 单一翻译文件包含所有键
   - TikMatrix Desktop: 多个 JSON 文件按插件分类（docs, blog, theme 等）

## 使用场景

### 添加新翻译键

当你在 Docusaurus 网站中添加新的 UI 文本时：

1. 运行 `npm run check` 查看哪些语言缺少新键
2. 使用 Docusaurus 的 `npm run write-translations` 命令生成新键
3. 手动翻译或使用翻译工具补充其他语言
4. 再次运行 `npm run check` 验证完整性

### 检查文档翻译进度

在翻译文档时：

1. 运行 `npm run check:docs` 查看文档完成度
2. 识别缺失或空的文档文件
3. 按优先级翻译重要文档
4. 定期运行检查确保质量

### CI/CD 集成

可以在 CI 流程中集成这些检查：

```yaml
# .github/workflows/check-i18n.yml
name: Check i18n Completeness
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: cd website/i18n-tools && npm install
      - run: cd website/i18n-tools && npm run check:all
```

## 依赖项

- `franc-min`: AI 驱动的语言检测库

## 注意事项

1. **语言检测准确性**：短文本（<15 字符）可能检测不准确，会标记为 "undefined"
2. **技术术语**：技术术语可能被误识别为英文，这是正常的
3. **文件大小阈值**：小于 100 字节的文件被认为是空文件
4. **性能**：检查大量文件时可能需要一些时间

## 贡献

如果你发现检测工具有任何问题或有改进建议，请提交 Issue 或 Pull Request。
