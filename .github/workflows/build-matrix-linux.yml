name: Build Matrix - Linux
run-name: ${{ github.actor }} is building Matrix (Linux) 🚀
on:
  workflow_call:
    inputs:
      version:
        description: "Version tag (e.g., v1.0.0)"
        required: true
        type: string
      branch:
        description: "Branch to build from"
        required: false
        default: "main"
        type: string
      build_apps:
        description: "Apps to build"
        required: false
        default: "all"
        type: string
      notes:
        description: "Release notes (English)"
        required: false
        default: ""
        type: string
      gray_percentage:
        description: "Gray release percentage (0-100)"
        required: false
        default: "10"
        type: string

permissions:
  contents: write

jobs:
  build-tikmatrix-pro:
    if: ${{ inputs.build_apps == 'all' || inputs.build_apps == 'tikmatrix.pro' }}
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout workflow repository
        uses: actions/checkout@v3
        with:
          path: ci-tools

      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: tikmatrix/tikmatrix-pro
          token: ${{ secrets.REPO_TOKEN }}
          ref: ${{ inputs.branch }}
          path: tikmatrix-pro

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: tikmatrix-pro/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev

      - name: Rust Cache
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ./tikmatrix-pro/target
          key: ${{ runner.os }}-cargo-tikmatrix-pro-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-tikmatrix-pro-
            ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: npm install
        working-directory: tikmatrix-pro

      - name: Build TikMatrix Pro package
        run: |
          chmod +x ./build-linux.sh
          ./build-linux.sh
        working-directory: tikmatrix-pro
        env:
          RUSTFLAGS: "-A warnings"
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Prepare official package upload
        run: |
          UPLOAD_DIR=./tikmatrix-pro/target/release/bundle/deb-upload
          mkdir -p "$UPLOAD_DIR"
          find ./tikmatrix-pro/target/release/bundle/deb -maxdepth 1 -type f -exec cp -t "$UPLOAD_DIR" -- {} +
          if ! find "$UPLOAD_DIR" -maxdepth 1 -type f -print -quit | grep -q .; then
            echo "No top-level deb artifacts found for upload"
            exit 1
          fi

      - name: Upload official package to R2
        uses: ryand56/r2-upload-action@v1.4
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID_NIO }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID_NIO }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY_NIO }}
          r2-bucket: matrix
          source-dir: ./tikmatrix-pro/target/release/bundle/deb-upload
          destination-dir: ./

      - name: Update TikMatrix Pro download url
        env:
          TIKMATRIX_API_KEY: ${{ secrets.TIKMATRIX_API_KEY }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          cd tikmatrix-pro
          node ./scripts/update-download-url.js --platform=linux --distributor=OFFICIAL --app=tikmatrix.pro --app-name='TikMatrix Pro'

  build-tikmatrix:
    if: ${{ inputs.build_apps == 'all' || inputs.build_apps == 'tikmatrix' }}
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout workflow repository
        uses: actions/checkout@v3
        with:
          path: ci-tools

      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: tikmatrix/tikmatrix-pro
          token: ${{ secrets.REPO_TOKEN }}
          ref: ${{ inputs.branch }}
          path: tikmatrix-pro

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: tikmatrix-pro/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev

      - name: Rust Cache
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ./tikmatrix-pro/target
          key: ${{ runner.os }}-cargo-tikmatrix-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-tikmatrix-
            ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: npm install
        working-directory: tikmatrix-pro

      - name: Build TikMatrix package
        run: |
          chmod +x ./build-linux.sh
          ./build-linux.sh tikmatrix
        working-directory: tikmatrix-pro
        env:
          RUSTFLAGS: "-A warnings"
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Prepare TikMatrix package upload
        run: |
          UPLOAD_DIR=./tikmatrix-pro/target/release/bundle/deb-upload
          mkdir -p "$UPLOAD_DIR"
          find ./tikmatrix-pro/target/release/bundle/deb -maxdepth 1 -type f -exec cp -t "$UPLOAD_DIR" -- {} +
          if ! find "$UPLOAD_DIR" -maxdepth 1 -type f -print -quit | grep -q .; then
            echo "No top-level deb artifacts found for upload"
            exit 1
          fi

      - name: Upload TikMatrix package to R2
        uses: ryand56/r2-upload-action@v1.4
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID_NIO }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID_NIO }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY_NIO }}
          r2-bucket: matrix
          source-dir: ./tikmatrix-pro/target/release/bundle/deb-upload
          destination-dir: ./

      - name: Update TikMatrix download url
        env:
          TIKMATRIX_API_KEY: ${{ secrets.TIKMATRIX_API_KEY }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          cd tikmatrix-pro
          node ./scripts/update-download-url.js --platform=linux --distributor=OFFICIAL --app=tikmatrix --app-name='TikMatrix'

  build-igmatrix:
    if: ${{ inputs.build_apps == 'all' || inputs.build_apps == 'igmatrix' }}
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout workflow repository
        uses: actions/checkout@v3
        with:
          path: ci-tools

      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          repository: tikmatrix/tikmatrix-pro
          token: ${{ secrets.REPO_TOKEN }}
          ref: ${{ inputs.branch }}
          path: tikmatrix-pro

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
          cache-dependency-path: tikmatrix-pro/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev

      - name: Rust Cache
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ./tikmatrix-pro/target
          key: ${{ runner.os }}-cargo-igmatrix-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-igmatrix-
            ${{ runner.os }}-cargo-

      - name: Install dependencies
        run: npm install
        working-directory: tikmatrix-pro

      - name: Build IgMatrix package
        run: |
          chmod +x ./build-linux.sh
          ./build-linux.sh igmatrix
        working-directory: tikmatrix-pro
        env:
          RUSTFLAGS: "-A warnings"
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Prepare IgMatrix package upload
        run: |
          UPLOAD_DIR=./tikmatrix-pro/target/release/bundle/deb-upload
          mkdir -p "$UPLOAD_DIR"
          find ./tikmatrix-pro/target/release/bundle/deb -maxdepth 1 -type f -exec cp -t "$UPLOAD_DIR" -- {} +
          if ! find "$UPLOAD_DIR" -maxdepth 1 -type f -print -quit | grep -q .; then
            echo "No top-level deb artifacts found for upload"
            exit 1
          fi

      - name: Upload IgMatrix package to R2
        uses: ryand56/r2-upload-action@v1.4
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID_NIO }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID_NIO }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY_NIO }}
          r2-bucket: matrix
          source-dir: ./tikmatrix-pro/target/release/bundle/deb-upload
          destination-dir: ./

      - name: Update IgMatrix download url
        env:
          TIKMATRIX_API_KEY: ${{ secrets.TIKMATRIX_API_KEY }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          cd tikmatrix-pro
          node ./scripts/update-download-url.js --platform=linux --distributor=OFFICIAL --app=igmatrix --app-name=IgMatrix
